name: test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: 0 4 * * SUN

jobs:
  test:
    uses: itzg/github-workflows/.github/workflows/go-test.yml@main
    with:
      go-version-file: go.mod
